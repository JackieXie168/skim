{
    IBClasses = (
        {
            ACTIONS = {closeProgress = id; done = id; dump = id; showFile = id; tryAgain = id; }; 
            CLASS = BibFiler; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                contextMenu = NSMenu; 
                forceCheckButton = NSButton; 
                iconView = NSImageView; 
                infoTextField = NSTextField; 
                progressCloseButton = NSButton; 
                progressIndicator = NSProgressIndicator; 
                progressSheet = NSPanel; 
                tv = NSTableView; 
                window = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = "java.lang.Object"; LANGUAGE = Java; }
    ); 
    IBVersion = 1; 
}