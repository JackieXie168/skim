{
    IBClasses = (
        {CLASS = BDSKDocument; LANGUAGE = ObjC; SUPERCLASS = NSPersistentDocument; }, 
        {
            ACTIONS = {
                addNewAutoGroup = id; 
                addNewFolderGroup = id; 
                addNewGroup = id; 
                addNewSmartGroup = id; 
                closeImportSettingsSheet = id; 
                editSmartGroup = id; 
                getInfo = id; 
                importFromBibTeXFile = id; 
                oneShotImportFromBibTeXFile = id; 
                showWindowForSourceListSelection = id; 
            }; 
            CLASS = BDSKMainWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                importSettingsMainBox = NSBox; 
                importSettingsWindow = NSWindow; 
                selectedItemsArrayController = NSArrayController; 
                sourceList = NSOutlineView; 
                sourceListTreeController = NSTreeController; 
            }; 
            SUPERCLASS = BDSKSecondaryWindowController; 
        }, 
        {
            ACTIONS = {addNewItem = id; }; 
            CLASS = BDSKSecondaryWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                currentDisplayController = id; 
                currentDisplayView = NSView; 
                searchField = NSSearchField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = NSPersistentDocument; LANGUAGE = ObjC; SUPERCLASS = NSDocument; }
    ); 
    IBVersion = 1; 
}