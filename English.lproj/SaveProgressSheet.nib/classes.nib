{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = SKDocument; 
            LANGUAGE = ObjC; 
            OUTLETS = {saveProgressBar = NSProgressIndicator; saveProgressSheet = NSPanel; }; 
            SUPERCLASS = NSDocument; 
        }
    ); 
    IBVersion = 1; 
}