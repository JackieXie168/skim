{
    IBClasses = (
        {
            ACTIONS = {
                changeFieldName = id; 
                changeFindExpression = id; 
                changeReplaceExpression = id; 
                findAndHighlight = id; 
                openHelp = id; 
                replaceAll = id; 
                replaceAndHighlightNext = id; 
                toggleCaseSensitivity = id; 
                toggleFindAsMacro = id; 
                toggleReplaceAsMacro = id; 
                toggleSearchType = id; 
                toggleSelection = id; 
            }; 
            CLASS = BDSKFindController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                caseSensitiveCheckbox = NSButton; 
                fieldToSearchComboBox = NSComboBox; 
                findAsMacroCheckbox = NSButton; 
                findTextField = NSTextField; 
                replaceAsMacroCheckbox = NSButton; 
                replaceButton = NSButton; 
                replaceTextField = NSTextField; 
                searchSelectionCheckbox = NSButton; 
                searchTypeMatrix = NSMatrix; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}