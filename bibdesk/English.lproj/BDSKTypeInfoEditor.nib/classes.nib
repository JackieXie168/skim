{
    IBClasses = (
        {
            ACTIONS = {
                addOptional = id; 
                addRequired = id; 
                addType = id; 
                cancel = id; 
                removeOptional = id; 
                removeRequired = id; 
                removeType = id; 
                revertAllToDefault = id; 
                revertCurrentToDefault = id; 
                saveChanges = id; 
            }; 
            CLASS = BDSKTypeInfoEditor; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                addOptionalButton = NSButton; 
                addRequiredButton = NSButton; 
                addTypeButton = NSButton; 
                optionalTableView = NSTableView; 
                removeOptionalButton = NSButton; 
                removeRequiredButton = NSButton; 
                removeTypeButton = NSButton; 
                requiredTableView = NSTableView; 
                revertAllToDefaultButton = NSButton; 
                revertCurrentToDefaultButton = NSButton; 
                typeTableView = NSTableView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = NSObject; LANGUAGE = ObjC; }, 
        {CLASS = OAExtendedTableView; LANGUAGE = ObjC; SUPERCLASS = NSTableView; }
    ); 
    IBVersion = 1; 
}