Refactoring Notes:

BibAuthor -- needs to be more of a firstclass object.
    BibItems should have arrays of bibauthor objects.
    BibAuthor objects should have first,von,last,jr accessors.
    BibAuthor Init should return a matching present instance, never duplicates.